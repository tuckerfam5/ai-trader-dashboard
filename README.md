# AI Trader dashboard

Static copy of the paper-trading dashboard from the private `AI Trader` project.
The page fetches its data from a gist that the trading cycle updates after every
run, so this repo only changes when the page itself changes. Paper money only.
